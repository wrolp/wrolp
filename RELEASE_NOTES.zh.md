# Wrolp Terminal 发行说明

[English](./RELEASE_NOTES.md) · [中文](#)

---

## v0.0.7 — 2026-09-07

新增串口（COM）与 Telnet 会话、内置 FTP/HTTP/TFTP 文件工具、可迁移的数据目录，以及基于文件的会话录制 —— 另附一轮 AI 与终端体验打磨。

### 新特性
- **串口终端** — 连接 COM 口，可配置波特率、数据位、校验位、停止位与流控；每个串口独占一个读取线程，并与 SSH 共用同一套终端、AI 与录制管线
- **波特率自动检测** — 扫描常用波特率并按置信度排序（跑在异步运行时之外，UI 不卡顿），另附常用波特率下拉与自定义值
- **Telnet 客户端** — 支持 IAC 协商（ECHO、SGA、TERMINAL-TYPE、NAWS）与自动登录；终端尺寸变化通过 NAWS 上报
- **网络扫描** — 扫描子网内存活的 SSH 与 Telnet 主机，并可直接从结果创建连接
- **内置文件传输工具** — FTP 服务器、带浏览器上传页的 HTTP/HTTPS 文件服务器、TFTP 服务器，以及 FTP / TFTP 客户端，全部从标题栏新增的「网络工具」按钮进入
- **HTTPS 自签名证书** — 一键为 HTTP 服务器生成证书与私钥
- **可迁移的数据目录** — 在设置里把全部应用数据（数据库、连接、录制、配置）迁到任意目录；下次启动时复制过去（需重启），并可选择把 `vault.key` 一并放在数据目录旁
- **基于文件的会话录制** — 录制事件以 NDJSON 追加写入 `<数据目录>/recordings/<工作空间>/<连接组>/<连接>/…jsonl`，不再写进 SQLite，使 `wrolp.db` 保持小巧、录制更易备份
- **录制管理** — 会话列表显示工作空间 / 连接组面包屑与「在文件夹中显示」；支持一键迁移仍存于数据库的历史录制、从录制目录重建索引的重新扫描，以及 asciinema v2（`.cast`）导出
- **AI 对话改进** — 可在输入框粘贴图片、点击缩略图看大图；「发送到终端」现在也支持本地 shell、串口与 Telnet 会话（只插入文本、不自动执行）；用对话模式选择器取代只读开关；端点设置收进折叠面板；agent 轮数可设为无限；Telnet 与串口会话也可使用 AI 对话
- **编辑器与界面** — 可配置在编辑器中打开文件的大小上限；容器名旁显示短容器 ID；本地终端使用对应 shell 的图标；更易抓取的细滚动条热区
- **数据库维护** — 设置页显示当前 `wrolp.db` 占用与其中可回收的空间，并提供「立即收缩」按钮；删除会话现在会自动把空闲页归还磁盘，而不是让数据库维持原大小

### 修复
- 避免为已打开的 COM 口重复创建终端标签
- 在串口标签上显示重连按钮
- 捕获命令时跳过分页提示，并在交互式提示处暂停实时着色
- 改进提示符 / 命令拆分与折行命令行的重新着色
- 命令面板透明度仅作用于背景
- 允许在提交数值前清空数值输入框
- 保持 pane 终端标签及其控件可见
- 避免波特率建议被裁切
- 点击遮罩不再关闭对话框，避免误点导致已输入内容丢失
- AI 回复（及其他 markdown）中的网页链接改为在系统浏览器中打开，而不是在应用自己的 WebView 里跳转，从而始终能回到应用

### 内部
- 把终端实现拆分为若干职责单一的辅助模块，并在各连接表单间复用同一个 `ClearableInput`
- 新增 Playwright 端到端测试套件，面向 mock 的 Tauri 后端驱动真实 UI
- 新增用户指南，并更新 README 以覆盖 Telnet、串口与隧道

---

## v0.0.6 — 2026-08-22

聚焦 SSH 保活可靠性、终端渲染质量与本地 shell 工作流。

### 新特性
- **可配置的 SSH 保活** — 在设置里配置保活间隔（最小 10 秒）与最大连续失败次数（最小 2 次）；持久化到 `window.json` 并应用于新连接
- **保活探测与「可疑」状态** — 通过新通道主动探测连通性（带超时）以尽早发现卡死连接；首次探测失败后标签变黄（「可疑」），恢复后变回绿色，连续失败达到上限后拆除连接
- **在文件管理器中打开** — 右键本地 shell 标签（跟随实时 `cd` 的工作目录）或侧边栏的本地终端条目，在系统文件管理器中打开其目录
- **pane 复制** — 右键分屏 pane 标签可将其克隆为新的分屏 pane，支持 SSH 会话、本地 shell 与 Docker 终端会话
- **本地 shell 显示名** — 具名本地 shell 条目会显示在标签上
- **终端渲染打磨** — 延迟「先适配再轮询」，使首帧输出按正确几何渲染，而不是先以一个过小的尺寸显示
- **内置 nerd 字体** — 随应用打包 MesloLG Nerd Font，修复终端与会话回放中的字形渲染

### 修复
- 在备用缓冲区程序（vim、htop 等）中跳过行重新着色，避免全屏界面被破坏
- 更新被跟踪的工作目录前先校验 `cd` 目标是否存在
- 高亮已输入命令时保留彩色的提示符
- 本地终端条目路径为空时打开用户主目录
- 提升状态提示文字的可读性
- 统一目录选择器行样式

### 内部
- 把 `commands.rs` 拆分为按领域划分的模块（ssh、window、recordings 等）
- 前后端代码格式化与清理

---

## v0.0.5 — 2026-08-18

聚焦终端 `ls` 集成、文件传输性能、SSH 隧道与 Docker 容器生命周期管理。

### 新特性
- **可点击的 `ls`/`dir` 输出** — 终端列表悬停提示 + 点击打开/进入，按真实工作目录精确解析路径，支持普通多列 `ls`、`ls -F` 与折行
- **嵌套会话文件浏览** — 可从 `docker exec` shell 与嵌套 SSH 会话打开文件、浏览目录，通过隐藏的 `pwd` 探测跟踪路径
- **终端输出高亮** — 已输入命令、表格输出、命令参数与管道后命令均着色；AI 下发的命令显示执行状态角标
- **文件面板局部刷新** — 新建、重命名、删除、上传与粘贴现在只刷新受影响的目录，并保留已展开的子树
- **远程复制/粘贴** — 跨目录复制/粘贴文件并处理冲突；可从剪贴板粘贴文件
- **自定义新建对话框** — 用应用内对话框替代原生 prompt 来新建文件/文件夹
- **拖拽上传目录** — 整目录上传并带视觉反馈
- **递归下载目录** — 通过 SFTP 下载远程目录
- **分块流式上传** — 大文件以 4 MB base64 分块流式传输，共用 SFTP 连接，Rust 侧 `walkdir` 目录流，并支持并行传输
- **传输取消** — 可取消进行中的传输与目录删除，并给出进度反馈
- **SSH 隧道** — 本地端口转发，支持已保存隧道的管理（增删改查）、转发失败上报与拒绝时的自动停止
- **Docker 容器生命周期** — 停止、启动与移除已停止的容器，操作后自动刷新容器列表
- **浮动命令片段列表** — 浮动命令片段面板，面板偏好可持久化，并支持追加到输入行
- **启动目录选项** — 可为每个 SSH 连接配置初始工作目录
- **编辑器保存提示** — 关闭有未保存更改的编辑器标签前先确认；语言选项按字母排序
- **位置跳转下拉** — 可跳转到常用位置（含本地磁盘根目录），并处理 Windows 盘符路径
- **按目标记忆浏览状态** — 文件面板的浏览状态在标签切换后保留

### 修复
- 修复折行 `ls` 的链接识别与悬停处理
- 在 `ls` 输出与终端 `cd` 跟踪中正确区分文件与目录（用于 shell 同步）
- 改进靠近顶部边缘时的链接提示定位
- 在 `cmdEcho` 中高亮命令参数并重着色管道后的命令
- 录制关闭时避免产生空会话
- 提升 SFTP 上传吞吐与 Windows 拖拽可靠性
- 修复传输行匹配与重复 key 问题
- 修复文件面板中新建按钮的点击位置与事件冒泡
- 刷新时正确保留用户已展开的目录状态
- 分屏 pane 失焦时仍保持浮层打开
- 操作后刷新 Docker 容器列表
- 把命令片段发送到获得焦点的终端 pane，并在发送后恢复焦点
- 降低空闲时的 SSH 轮询与重连开销
- 在 README 中加入致谢章节

---

## v0.0.4 — 2026-08-08

新增本地终端支持、浮动 pane、二进制文件查看与 AI 工作流改进。

### 新特性
- **本地终端** — 在本机打开 PTY 后端 shell，与远程 SSH 标签并列，并完整支持 AI
- **浮动 pane** — 把任意面板（编辑器、Docker 日志、十六进制查看器）拖出为独立浮动窗口
- **十六进制查看器** — 以格式化 hex dump + ASCII 侧栏查看二进制文件
- **图片预览** — 在文件面板中直接查看图片（PNG、JPG、GIF、WEBP）
- **AI 提示词模板** — 内置模板分类，自定义模板可在对话输入框下拉选择
- **「发送到终端」** — 把 AI 生成的代码块或选中文本直接发送到当前终端
- **敏感命令需用户确认** — AI 在执行危险操作（如 `rm -rf`、`docker system prune`）前先请求确认
- **编辑上一条消息** — 修改并重发上一条 AI 对话消息，保留工具调用历史
- **取消进行中的 AI 流** — 可在流式输出中途停止 AI 回复
- **总在最前开关** — 把窗口固定在其他窗口之上
- **Toast 通知** — Docker 容器重启等事件显示简短通知
- **文件面板显示服务器名** — 在文件管理器头部显示已连接主机名
- **本地终端条目** — 在设置中配置具名本地 shell 条目
- **HEX/图片查看器并入标签头** — 在文件视图间无缝切换
- **改进的 AI 对话体验** — react-markdown 渲染、代码块复制按钮、精简为纯图标的复制按钮
- **Docker 日志自动滚动** — 裁剪日志时智能锚定滚动位置
- **按 pane 的会话录制开关** — 可为每个终端 pane 单独开关录制；另设全局自动录制开关

### 修复
- 按助手消息持久化工具调用，并在发送后重新聚焦输入框
- AI 对话标签未挂载远程 shell 时回退到本地执行
- 按 SSH 会话隔离编辑器与日志标签，避免跨会话串扰
- 更新标题栏图标为 Feather 风格并调整间距
- 支持 Log4j 风格时间戳并改进 ANSI 颜色映射
- 避免右键菜单被裁到屏幕外
- 修复图片与十六进制查看器的拖拽调整手柄

---

## v0.0.3 — 2026-08-02

聚焦国际化、AI 增强与 Docker 体验。

### 新特性
- **国际化支持** — 完整的中文与英文（zh）本地化；可在设置中切换语言
- **AI 端点与模型选择器** — 可为每个对话会话配置 OpenAI 兼容端点与模型
- **自动注入服务器上下文** — AI 自动获得当前服务器信息；新增 `get_current_server` 工具
- **按标签的 AI 对话面板** — 停靠或浮动的 AI 对话窗口，每个 shell 标签一个
- **拖拽重排标签** — 拖动标签栏重排标签
- **主机分析面板** — 从远程主机采集 CPU、内存、磁盘与网络信息
- **Docker 日志查看器增强** — ANSI 颜色解析、右键「询问 AI 助手」、跳到底部按钮
- **Docker compose 识别** — 识别并显示 compose 项目上下文
- **Docker 容器分析** — 查看容器配置、环境变量、卷与网络
- **终端回滚缓冲控制** — 可配置最大回滚缓冲并可清空
- **AI 设置重构** — 分页式设置布局，独立的 AI 配置区与新图标
- **AI 对话状态持久化** — 对话历史在标签切换后保留
- **可配置的 Docker 日志偏好** — 行数上限、跟随模式、自动滚动
- **自动化发布流水线** — 构建、签名与发布脚本
- **中文 README** — 项目文档的完整中文翻译

### 修复
- 去掉流式 AI 文本开头的空白
- 改进停靠面板的调整行为并避免干扰终端
- 修复多列 `ls` 与 `ls -F` 输出渲染
- 确保「编辑」能正确替换消息并清空工具历史
- 切换端点时同步 API Key 输入框
- 避免启动期间的终端 resize 报错

---

## v0.0.2 — 2026-07-28

在布局、文件管理与安全方面带来显著的界面与功能改进。

### 新特性
- **分屏终端 pane** — 水平或垂直拆分 SSH 会话，分隔条可拖动调整
- **pane 重排** — 拖拽标签与 pane 重新排列布局
- **面板停靠** — 把任意面板（文件管理器、AI 对话、Docker 日志）停靠到左、右或底部
- **密码可见性开关** — 在连接表单中显示/隐藏密码
- **连接凭据静态加密** — 使用操作系统密钥链安全存储 SSH 密码与 API Key
- **统一的远程文件系统** — SSH 与 Docker 目标共用同一套文件系统抽象
- **文件传输进度** — 上传/下载实时进度条，支持暂停与恢复
- **Monaco 编辑器增强** — 支持 nginx 配置与 properties 文件、minimap 开关、更细的滚动条
- **内联远程编辑** — 可从终端或文件面板直接打开远程文件
- **会话录制与命令集** — 录制终端会话并保存可复用的命令组
- **SSH 重连** — 失效会话自动重连，并带单调递增的会话 ID 防护
- **拖拽上传文件** — 把文件拖到文件面板即可上传
- **可编辑的文件路径输入** — 直接输入路径，无需逐层浏览
- **工作空间布局持久化** — 重启后记住分屏位置与面板状态
- **终端状态栏** — 显示每个 pane 的尺寸
- **删除全部会话** — 带确认对话框的批量清理
- **应用图标更新** — 用精细的 SVG 图标集替换 emoji 图标
- **Tauri 打包** — Windows 的 MSI 安装包（也提供 Linux/macOS 构建）

### 修复
- 把调整手柄约束在图片边界内
- 抑制辅助通道输出泄漏到终端
- 默认关闭 minimap 以减少视觉干扰
- 修复右键菜单在视口边缘被裁切
- 修复 SSH 密钥路径的 `~` 展开，并默认使用 `~/.ssh/id_rsa`
- 修复切换编辑器浮层时的标签重挂载
- 修复 Docker 面板的调整方向

---

## v0.0.1 — 2026-06-28

首个版本。

### 新特性
- **多标签 SSH 终端** — 基于 xterm.js，支持密码与 SSH 密钥认证
- **远程文件管理** — 基于 `russh-sftp` 的 SFTP 文件浏览器，支持上传/下载
- **远程文件编辑器** — 基于 Monaco 的编辑器，自动识别 UTF-8/GBK 编码
- **会话录制** — 自动把所有终端会话录制到 SQLite，并可在底部面板回放
- **命令集** — 保存并复用常用命令组
- **标签管理** — 拖拽重排标签、复制标签、标签右键菜单
- **尺寸调整与布局** — 侧边栏可调宽度、支持分屏、面板可停靠/浮动
- **Docker 集成** — 查看容器、查看日志、进入容器 shell
- **主机分析** — 从远程主机采集系统信息
- **AI 助手** — OpenAI 兼容对话集成，支持流式输出、工具调用 agent 循环与多模态（图片）输入
- **系统托盘** — 最小化到托盘，通过托盘图标显示/隐藏/退出
- **自动更新** — 自动检查并安装更新
- **窗口控制** — 自定义标题栏、透明/背景模式、窗口几何持久化
- **加密密钥** — 连接凭据静态加密
